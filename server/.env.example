# ============================================================
# 本地路径配置模板
# 用法：复制本文件改名为 .env，再按你电脑实际路径修改
#   Copy-Item .env.example .env
#   notepad .env
# .env 不会提交到 git（含个人路径）
# 启动脚本已带 --env-file-if-exists=.env，文件不存在也能正常启动
# 优先级：命令行手动设置 > 本文件 > 代码里的默认值
# ============================================================

# 照片库根目录（iCloudPD 拉下来的源文件，只读）
# 默认：项目根\iCloudPhoto
# PHOTO_LIBRARY=F:\iPhone\icloud-app\iCloudPhoto

# 缓存目录（缩略图 / 视频封面 / 转码产物，可删除重建）
# 默认：项目根\server\cache
# CACHE_DIR=F:\iPhone\icloud-app\server\cache

# SQLite 数据库文件
# 默认：项目根\server\cache\library.db
# DB_PATH=F:\iPhone\icloud-app\server\cache\library.db

# 后端端口（前端 5173 通过代理访问）
# 默认：8899
# PORT=8899

# 扫描上限（调试用，0=全量）
# SCAN_LIMIT=0
